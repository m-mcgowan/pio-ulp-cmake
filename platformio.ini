; pio-ulp-cmake — Full CMake ULP builds for PlatformIO + ESP-IDF
;
; This is NOT a buildable project. Build the examples instead:
;
;   cd examples/ulp_riscv_gpio && pio run
;   cd examples/lp_core_build_system && pio run
;
; Or run the integration tests:
;
;   ./tests/test_ulp_cmake.sh
;
; See examples/ for self-contained PIO projects using verbatim ESP-IDF
; ULP examples. See tests/fixture/ for the multi-project test harness.
