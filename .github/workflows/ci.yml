# SPDX-FileCopyrightText: 2026 Mat McGowan
# SPDX-License-Identifier: MIT

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"  # Weekly Monday 6am UTC

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install lint tools
        run: pip install flake8

      - name: flake8
        run: flake8 scripts/

      - name: ShellCheck
        uses: ludeeus/action-shellcheck@master
        with:
          scandir: scripts tests
          severity: warning

  # Install platform + framework once per version, then cache for downstream jobs
  setup:
    name: "Setup (${{ matrix.platform_version }})"
    runs-on: ubuntu-latest
    needs: lint
    strategy:
      matrix:
        platform_version:
          - "55.03.37"   # IDF 5.5.2
          - "54.03.21-2" # IDF 5.4.2
    env:
      PLATFORM_URL: "https://github.com/pioarduino/platform-espressif32/releases/download/${{ matrix.platform_version }}/platform-espressif32.zip"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache PlatformIO
        id: cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio/packages
            ~/.platformio/platforms
          key: pio-${{ runner.os }}-${{ matrix.platform_version }}

      - name: Install PlatformIO and platform
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          pip install platformio
          sed -i "s|platform = .*|platform = $PLATFORM_URL|" \
            tests/fixture/platformio.ini
          cd tests/fixture && pio pkg install

  test-fixture:
    name: "Test fixture (${{ matrix.platform_version }})"
    runs-on: ubuntu-latest
    needs: setup
    strategy:
      fail-fast: false
      matrix:
        platform_version:
          - "55.03.37"   # IDF 5.5.2
          - "54.03.21-2" # IDF 5.4.2
    env:
      PLATFORM_URL: "https://github.com/pioarduino/platform-espressif32/releases/download/${{ matrix.platform_version }}/platform-espressif32.zip"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Restore PlatformIO cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio/packages
            ~/.platformio/platforms
          key: pio-${{ runner.os }}-${{ matrix.platform_version }}

      - name: Install PlatformIO
        run: pip install platformio

      - name: Override platform version
        run: |
          sed -i "s|platform = .*|platform = $PLATFORM_URL|" \
            tests/fixture/platformio.ini

      - name: Run integration tests
        run: ./tests/test_ulp_cmake.sh -v

  build-examples:
    name: "Build examples (${{ matrix.platform_version }})"
    runs-on: ubuntu-latest
    needs: setup
    strategy:
      fail-fast: false
      matrix:
        platform_version:
          - "55.03.37"   # IDF 5.5.2
          - "54.03.21-2" # IDF 5.4.2
    env:
      PLATFORM_URL: "https://github.com/pioarduino/platform-espressif32/releases/download/${{ matrix.platform_version }}/platform-espressif32.zip"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Restore PlatformIO cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio/packages
            ~/.platformio/platforms
          key: pio-${{ runner.os }}-${{ matrix.platform_version }}

      - name: Install PlatformIO
        run: pip install platformio

      - name: Sync examples from framework
        run: ./scripts/sync_examples.sh

      - name: Override platform version in examples
        run: |
          for ini in examples/*/platformio.ini; do
            sed -i "s|platform = .*|platform = $PLATFORM_URL|" "$ini"
          done

      - name: Build all examples
        run: |
          for dir in examples/*/; do
            example="$(basename "$dir")"
            echo "=== Building: $example ==="
            (cd "$dir" && pio run)
            echo ""
          done

  package:
    name: Package
    runs-on: ubuntu-latest
    needs: [test-fixture, build-examples]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: Extract version from CHANGELOG
        id: version
        run: |
          # Get the first version heading (latest release)
          version=$(grep -oP '(?<=## \[)\d+\.\d+\.\d+' CHANGELOG.md | head -1)
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "Version: $version"

      - name: Create release archive
        run: |
          version="${{ steps.version.outputs.version }}"
          staging="pio-ulp-cmake-${version}"
          mkdir -p "$staging"

          cp scripts/pio_ulp_cmake.py "$staging/"
          cp README.md "$staging/"
          cp LICENSE "$staging/"
          cp CHANGELOG.md "$staging/"

          tar czf "pio-ulp-cmake-${version}.tar.gz" "$staging"

          # Also keep the standalone script as a separate artifact
          cp scripts/pio_ulp_cmake.py "pio_ulp_cmake-${version}.py"

      - name: Upload release archive
        uses: actions/upload-artifact@v4
        with:
          name: release-archive
          path: pio-ulp-cmake-*.tar.gz
          retention-days: 90

      - name: Upload standalone script
        uses: actions/upload-artifact@v4
        with:
          name: release-script
          path: pio_ulp_cmake-*.py
          retention-days: 90
